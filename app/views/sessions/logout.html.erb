<div class="page-header">
<h3><%= t('page.user.logout.subheader') %></h3>
</div>
<p>
<%= t('page.user.logout.message') %>
</p>
<%= link_to t('logout'), session_path(current_user), class: 'btn btn-lg btn-info', method: :delete %> 
<%= link_to t('cancel'), root_path  %>