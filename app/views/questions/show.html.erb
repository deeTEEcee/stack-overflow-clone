<% answers = @question.answers %>

<div class="page-header">
<h1><%= @question.title %></h1>
</div>

<div id="question">
	<%= @question.description %>
	TODO; insert edit here as well
</div>

<div id="answers">
<% if answers.exists? %>
		<h4> <%= answers.count %> Answers </h4>
		<hr>
		<% answers.each do |answer| %>
			<div id="answer">
				<%= answer.description %>
				<hr>
			</div>
		<% end %>
<% else %>
	Know someone who can answer? Share a link to this TODO: question via TODO:email, , , or .
	<hr>
<% end %>
</div>

<%= render 'answers/form' %>
