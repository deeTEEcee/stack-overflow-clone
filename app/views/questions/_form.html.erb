<%= simple_form_for(@question) do |f| %>
  <%= f.input :title, placeholder: "What's your programming question? Be specific." %>
  <%= f.input :description, as: :text %>
  <%= f.button :submit, 'Post Your Question', class: 'btn btn-md btn-primary' %> 
<% end %>